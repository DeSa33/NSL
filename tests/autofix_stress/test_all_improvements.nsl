# COMPREHENSIVE AUTOFIX TEST - ALL IMPROVEMENTS

# 1. Missing commas
let nums = [1, 2, 3, 4, 5]
let obj = { name: "test", age: 25, active: true }
fn calculate(x, y, z) { return x + y + z }

# 2. Keyword typos
fn broken() { return 5 }
let a = true
let b = null
let c = false

# 3. Wrong operators
if x == 5 { }
if y != 10 { }

# 4. Wrong keywords
let count = 10
struct Person { name: string }
trait Drawable { draw():  }

# 5. Multi-parameter lambda
fn lambda_24(x, y) { return x + y }
fn lambda_25(a, b, c) { return a * b + c }

# 6. Python-style blocks with closing braces
if score > 90 {
  print("A")
} else if score > 80 {
  print("B")
} else if score > 70 {
  print("C")
} else {
  print("F")
}

# 7. For loop
for i in range(10) {
  print(i)
}

print("all tests complete")

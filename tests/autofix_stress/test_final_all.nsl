# FINAL COMPREHENSIVE AUTOFIX TEST

# 1. Missing commas
let nums = [1, 2, 3, 4, 5]
let obj = { name: "test", age: 25 }
fn calc(x, y, z) { return x + y + z }

# 2. Keyword typos
fn broken() { return 5 }
let a = true
let b = null

# 3. Wrong operators
if x == 5 { }

# 4. Wrong keywords
let count = 10
struct Person { }

# 5. Multi-parameter lambda
fn lambda_21(x, y) { return x + y }

# 6. Python blocks with closing braces
if score > 90 {
  print("A")
} else if score > 80 {
  print("B")
} else {
  print("F")
}

print("done")

# Edge case tests v2 - simpler cases

# 1. Single-level Python blocks (should work)
if score > 90 {
  print("A")
} else if score > 80 {
  print("B")
} else {
  print("C")
}

# 2. Simple for loop
for i in range(5) {
  print(i)
}

# 3. Simple while loop
while x > 0 {
  x = x - 1
}

# 4. Mixed content in objects
let config = { host: "localhost", port: 8080, debug: true }

# 5. Function with params
fn process(data, items, count) { return data }

# 6. Multiple keyword issues
fn test(a, b) { return true }

# 7. Empty brackets (should not modify)
let empty = []
let emptyObj = {}

# 8. Already correct
let correct = [1, 2, 3]

print("edge cases v2 complete")

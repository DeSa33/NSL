# COMPREHENSIVE AUTOFIX TEST

# Missing commas
let nums = [1, 2, 3]
let obj = { a: 1, b: 2 }
fn add(x, y) { return x + y }

# Keyword typos
fn test() { return 5 }
let a = true
let b = null

# Wrong operators
if x == 5 { }

# Wrong keywords
let count = 10
struct Person { }
fn lambda_19(x) { return x * 2 }

print("done")

# FINAL AUTOFIX TEST

# Missing commas in arrays
let nums = [1, 2, 3]

# Missing commas in objects
let obj = { a: 1, b: 2 }

# Missing commas in params
fn add(x, y) { return x + y }

# Keyword typos
fn test() { return 5 }
let a = true
let b = null

# Operators
if x == 5 { }

# Keywords
let count = 10
struct Person { }
fn(x) { return x * 2 }

print("done")
